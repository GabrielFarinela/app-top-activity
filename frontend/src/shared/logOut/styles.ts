import styled from "styled-components";

export const Logout = styled.span`
   position: absolute;
   color: white;
   top: 32px;
   cursor: pointer;
   right: 35px;

   &:hover{
      text-decoration: underline;
      opacity: 0.8;
   }
`;